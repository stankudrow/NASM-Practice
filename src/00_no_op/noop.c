/*
 * The C version of `noop.asm` program;
 *
 * The `main` function is the entry point for the program
 * like the `_start` label in Assembly (for "ld" linker for sure).
 */

#define SUCCESS_RETCODE 0

int main()
{
    return SUCCESS_RETCODE;
}
